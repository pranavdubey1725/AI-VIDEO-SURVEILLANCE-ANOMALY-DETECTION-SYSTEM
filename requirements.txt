# Core ML
# PyTorch — install separately BEFORE running pip install -r requirements.txt:
#   GPU (CUDA 12.4): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   CPU only:        pip install torch torchvision torchaudio
# The lines below are pinned to the GPU version used during development.
# On CPU, install PyTorch first (command above), then run this file.
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# Computer vision & detection
ultralytics==8.4.47
opencv-python==4.13.0.92
Pillow

# Data
numpy==2.0.2
pandas==2.3.3
scikit-learn==1.6.1
tqdm==4.67.3

# Visualisation
matplotlib==3.9.4
seaborn==0.13.2
plotly==6.7.0

# API backend
fastapi==0.128.8
uvicorn==0.39.0
python-multipart==0.0.20
requests==2.32.5

# Frontend is served as static files by FastAPI (no separate framework needed)
